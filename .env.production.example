# Vercel: set these in Project Settings > Environment Variables (Production).
# The canonical URL must match the production domain.
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://albertoavendano.com
SHOW_DEMO_BLOG_POSTS=false
